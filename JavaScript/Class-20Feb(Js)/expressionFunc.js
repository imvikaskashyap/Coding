// Expression functions are anonymous functions that can be used to create a single expression. They are often used to simplify complex code and make it easier to read.
// expression function two type ke hote hain
// 1. named expression func.
// 2.anonymous fuunc.

// Syntax:
// (parameters) => expression

const addTwoNumbers = (a, b) => a + b; // This is an expression function that takes two parameters and returns the sum of them.

const expression = (x, y) => {
	return x + y;
};
console.log(expression(2, 3)); // Outputs 5
